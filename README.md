# Queue_Linked_List
This C++ program implements a Queue data structure using a Singly Linked List. While the code is labeled as a queue, its current logic actually mimics a hybrid structure because of how the insertion and deletion functions are defined.
